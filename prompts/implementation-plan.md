# Implementation Plan Prompt

Use this after `PRODUCT.md` exists.

```text
Read PRODUCT.md and the selected template.

Create a short implementation plan with:

- pages and routes
- components
- data model and migrations
- server actions or API routes
- external integrations
- acceptance checks

Then implement the MVP in small steps. Keep the template structure intact unless
there is a clear reason to change it.
```
